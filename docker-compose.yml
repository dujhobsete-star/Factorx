services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: factorx
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["factorx_data:/var/lib/postgresql/data"]
volumes:
  factorx_data:
